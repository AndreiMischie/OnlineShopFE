export interface Product {
  name: string;
  category: string;
  price: number;
  description: string;
  quantity: number;
  id: string;
}

export interface Products {
  name: string;
  productCategoryName: string;
  price: number;
  description: string;
  id: string;
}
